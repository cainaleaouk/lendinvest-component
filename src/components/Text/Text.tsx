import styled from 'styled-components';
import { Colors } from '../../themes';

//TODO: add font-family here when we find out what it's suppose to be
export const Text = styled.p`
    font-size: 14px;
    margin: 0;
    padding: 0;
    color: ${Colors.GREY_DARK};
`;